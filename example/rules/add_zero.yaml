rules:
  - id: add-zero-fold
    message: "Replace x + 0 with x"
    languages: [c, cpp]
    severity: INFO
    patterns:
      - pattern-either:
        - pattern: 0 + $X
        - pattern: $X + 0
    fix: $X
